package <empty> {
  class Test(x: Int, y: Double) extends Object() { this: Test => 
    private[this] val x: Int
    private[this] val y: Double
    def <init>(v: Int): Unit = {
      this(v, 5.0)
      ()
    }
  }
}